/**
 * Application-facing terminal session port for dashboard UIs.
 *
 * @module react/TerminalSessionPort
 */

export type {
  OpenTerminalSession,
  TerminalEvent,
  TerminalSessionId,
  TerminalSessionPort,
} from "../Terminal.js";
