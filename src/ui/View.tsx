/**
 * @module ui/View
 *
 * Re-export of `last-ts/View` — Effect DI components only
 * (`Tag` / `Prototype` / `provide` / `Chrome`). Dashboard contribution surface:
 * {@link ./Views}.
 */
export * from "last-ts/View";
