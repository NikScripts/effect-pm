/**
 * Re-export of last-ts asRoutes brand — Group.asRoutes + Route.fromEffect share it.
 */
export {
  AsRoutesTypeId,
  AsRoutesItems,
  isAsRoutesBrand,
  type AsRoutesBrand,
  type AsRoutesEffect,
  type RouteGroup,
} from "last-ts/Route";
