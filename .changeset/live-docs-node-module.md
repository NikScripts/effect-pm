---
"hyperlink-ts": patch
---

Live book uses `Node.Tag` / `Node.httpServer` (not removed `Resource.Node` / `Resource.httpServer`).

Updates guides, getting-started, index, readiness/dashboard/standards, and Resource module TSDoc. Handoffs/archive left as historical.
