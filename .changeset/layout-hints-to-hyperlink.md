---
"last-ts": minor
"hyperlink-ts": minor
---

Move pane metadata off last-ts: `LayoutHints` / `LayoutHintsProvider` / `useLayoutHints` now live on `hyperlink-ts/ui/View` (TUI/dashboard). last-ts `View` is DI only — per codesplit lock.
