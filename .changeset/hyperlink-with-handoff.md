---
"hyperlink-ts": minor
---

Opt-in per-HyperService cutover: `Hyperlink.withHandoff("drain-only" | "workPool-release")` runs during `Node.shutdown` after drain and before Lookup leave (default off).
