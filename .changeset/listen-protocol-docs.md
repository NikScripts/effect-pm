---
"hyperlink-ts": patch
---

**Docs/examples — protocol listen:** TSDoc and forms examples teach `Node.unix` / `Node.http` / `Node.ws` for transport bind; neutral `Node.listen` is documented as non-binding. Added nameless Http/WebSocket form examples.
