---
"hyperlink-ts": minor
---

Fold interface-driven tags into `Hyperlink.Tag<Self, I>()` (remove `fromService`). Rename materialize `constant(Schema)` → `value(Schema)` with optional acquire error (fails layer/client build).
