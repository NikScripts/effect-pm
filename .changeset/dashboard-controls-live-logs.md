---
"@nikscripts/effect-pm": minor
---

Add headless React dashboard primitives for adaptive controls and live logs.

This introduces browser-safe dashboard target types, `<Controls for={...} />`, `<Logs for={...} />`, `useControlPlaneLogs`, and `ControlPlanePort.logs(...)` with live-following NDJSON log streams plus bounded history parameters.
