---
"last-ts": minor
---

`Page.make` / `Page.static` — HttpApi-shaped page classes (optional request options first). `Route.fromEffect` / `Route.staticFromEffect` lift literal param bags into the route (get stays dynamic by default; staticFromEffect opts into SSG). Shared `RequestOptions` bag with `Route.get`.
