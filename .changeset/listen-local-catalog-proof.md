---
"hyperlink-ts": patch
---

**`Node.listenLocal` carries the same catalog proof as `listen`** — serve list must cover the node's `ROut`; success / services propagate instead of `Layer<never, unknown, unknown>`.
