---
"@nikscripts/effect-pm": minor
---

Integrate parallel agent work onto `cursor/integration-jun-2026`: dashboard controls and ops-ui shell, control and log Effect RPC transports, command authentication, remote terminal session contracts, and queue facet telemetry emit helpers.

This release line also bumps Effect to `4.0.0-beta.75`. See sibling changesets in this merge for per-feature notes (dashboard, RPC, terminal, log transport, queue emit).
