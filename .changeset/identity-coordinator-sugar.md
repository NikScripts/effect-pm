---
"@nikscripts/effect-pm": patch
---

**Identity coordinator sugar (M6)** — recipe guide + lean prefer helpers.

- Guide: `docs/guides/identity-coordinator.md` (one brain, many hands).
- `Lookup.prefer(tag|key, nodeKey)` and `Lookup.preferEntry(resourceKey, entry)`.
- Clearer `IdentitySelfRequired` message (Lookup pipe + dialable self).
- Form uses `Lookup.prefer`; PACKAGE-GUIDE / examples README cross-links.
