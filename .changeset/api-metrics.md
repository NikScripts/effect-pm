---
"hyperlink-ts": patch
---

Add `ApiMetrics` observability resource (`Resource.tagFor` factory) linked to `HttpApiResource.Service` by `clientId`, with windowed `metrics` stream and `usageNow` query. Rename/add `HttpApiResource.Service` class factory with endpoint usage metrics and registry hooks.
