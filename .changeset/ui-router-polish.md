---
"hyperlink-ts": minor
---

**UI Router polish:** `Route.targetOf` / `TargetValue`, `urlBuilder({ baseUrl })`, `Service.crumbs`, `Router.useMatch` / `useTarget`, `View.compose` accepts `Layer | Service`.
