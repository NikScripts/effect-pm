---
"last-ts": minor
---

`Last.provide(Service | Effect, requirements?)` fulfills at the page edge (success value / ViewFn). Prefer `View.stamp(Last.provide(…))` for a JSX `Component` brand. Bag `yield* Last.provide` / `Last.toLayer` removed (Document owns title).
