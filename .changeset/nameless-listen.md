---
"hyperlink-ts": minor
---

**Nameless `Node.listen([serve…])`** — mint address-less Node, D7 claim, ipc bind; **Lookup stays in `R`** (provide it, or use `Node.unix` for batteries). Dial with `Resource.clientLocal`.
