Orphaned changeset files moved here so they are **not** consumed by `changeset version`.
They described work that was already folded into prior commits or superseded by
`process-v2-polling-schedule.md` for the **0.7.0-beta.0** line.
