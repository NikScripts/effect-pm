---
"hyperlink-ts": minor
---

**UI Route (HttpApi-shaped):** Single `hyperlink-ts/ui/Route` namespace — `make` / `group` / `app` / `match` / `urlBuilder`. Group → route reflection is `groupRoute.routes` (bridge; same Route builders). Removed `ui/Routes`. Navigator cutover deferred.
