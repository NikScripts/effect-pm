---
"hyperlink-ts": minor
---

Add `Observe` (`hyperlink-ts/Observe`) — unbound recipes/packs with `bind` / `use` — and `WorkPoolView.pack` (queue UI pack on the service View module). Prefer `Observe.use(Jobs, WorkPoolView.pack)` over `Bundle.observe` for new code.
