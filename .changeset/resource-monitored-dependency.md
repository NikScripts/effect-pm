---
"@nikscripts/effect-pm": minor
---

**`Resource.monitoredDependency`** — small factory for the repeated dependency-monitor shape: `status` effect + `changes` stream + readiness from `readyWhen` / optional `detail`. Still a plain `Resource.Tag` contract (not a new kind). Use with `withReadiness`.
