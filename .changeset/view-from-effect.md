---
"last-ts": minor
---

**`View.fromEffect`:** plain Effect → exportable React component (no Tag / DI). Runtime from `AtomReact.RuntimeProvider` — no runtime argument. Keep `"use client"` on the app module that exports the result.
