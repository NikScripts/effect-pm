---
"hyperlink-ts": minor
---

**View DI:** `View.Tag` / **`View.Prototype`** — component Context.Service handles (props from Prototype; Self = DI identity; `View.Type<typeof Tag>`). Size chrome add-on: **`View.Card` / `Detail` / `Page` prototypes** stamp `size`. Matchers moved off the root namespace onto **`View.react` / `compose` kits** (`ui.Card`) and **`View.useMatch()`**. Chrome registration: `View.bind` + `View.only`.
