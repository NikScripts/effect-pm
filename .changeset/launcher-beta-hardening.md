---
"hyperlink-ts": patch
---

Launcher beta hardening: CSPRNG assume tokens, `HandleNotReady` before handoff, richer tagged-error messages, and Effect log spans/annotations on spawn / Ready / handoff / `Node.assume` (tokens never logged).
