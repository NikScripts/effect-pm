---
"hyperlink-ts": minor
---

**Update module** — `Update.plan` → `Update.simulate` → `Update.execute` for
ordered fleet cutovers with optional contract from→to audit. Replaces
options-bag `Launcher.restartSuccessor` as the preferred app API (`hyperlink-ts/Update`).
Also completes Tag→Service renames for Dialers + Lookup sibling Context keys left
broken after the Effect v4 mint rename.
