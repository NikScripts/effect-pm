---
"hyperlink-ts": patch
---

Unit-test F4 `contractHash` stability / drift (same Spec, payload change, added method).

Also records an `anyUnknownInErrorContext` inventory under `docs/plans/` (rule still off) and closes the HttpApi transport-conformance row as N/A in the loud-failures handoff.
