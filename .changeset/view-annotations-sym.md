---
"last-ts": minor
"hyperlink-ts": patch
---

**View handle slots:** Prototype metadata stamps under `View.annotationsSym` — read only via `View.annotations(tag)` (not `Tag.annotations`). Factory brand `View.kind` (`"last-ts/View"`) stamps under `Last.kindSym`; read with `Last.kindOf(tag)`. New subpath `last-ts/Last`.
