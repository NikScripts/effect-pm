---
"hyperlink-ts": patch
---

`@nikscripts/effect-pm/web`: log timestamps use a compact 24-hour clock (`HH:MM:SS`, no AM/PM) with `whitespace-nowrap` + `tabular-nums`, so the time column no longer wraps. Applied to the log stream and the debug console.
