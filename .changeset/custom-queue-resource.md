---
"@nikscripts/effect-pm": minor
---

Add `CustomQueueResource` and `CustomQueueContract` for N-level queues with named lanes, `add(item, level?)`, and `Record<string, number>` sizes. Includes `Resource.mutatePair` for two-argument wire methods. Subpaths: `@nikscripts/effect-pm/CustomQueueResource`, `@nikscripts/effect-pm/CustomQueueContract`.
