---
"@nikscripts/effect-pm": patch
---

Docs: add `docs/guides/setup.md` — a consumer setup guide (install + peer-dep matrix, the subpath map, and wiring a `serveAllHttp` server + `/cli` / `/tui` / `/web` dashboards against your tags) for apps adopting the toolkit.
