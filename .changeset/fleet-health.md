---
"hyperlink-ts": minor
---

FleetHealth — meshed stadium-board health (`local` / `byNode` / `status`) with Reachable vs Unreachable from Effect Exit; MultiNode.combineByNodeExit keeps peer failures.
