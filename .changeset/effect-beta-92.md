---
"@nikscripts/effect-pm": patch
---

Upgrade Effect to `4.0.0-beta.92` (from `beta.69`). Bumps `effect` (peer + dev), `@effect/platform-node`, `@effect/sql-sqlite-node`, and `@effect/vitest` to `4.0.0-beta.92` in lockstep. Typecheck, build, and the full test suite pass unchanged. Consumers should move to `effect@^4.0.0-beta.92`.
