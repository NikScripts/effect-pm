---
"hyperlink-ts": minor
---

Add `View` registry skeleton on `hyperlink-ts/ui`: `View.make` / Layer `register` + `bind*` / `View.react` → `{ Card, Detail, Provider, useView, resolve }`. Spec-keyed chrome matching (annotation → tag → factory → kind → fallback); Dashboard migration off `forKind` still follow-up.
