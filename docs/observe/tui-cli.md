{#tui-cli title="TUI & CLI" status="draft" appliesTo=all}
# TUI & CLI

{.note}
**Placeholder — outline only.** Full content to come; this page exists now so other pages can link to it.

The CLI (`Hyperlink.cli` / `hyperlink-ts/cli`) and the terminal (TUI) dashboard.
