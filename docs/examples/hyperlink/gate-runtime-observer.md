{#gate-runtime-observer title="Gate — runtime observer" status="draft" appliesTo=all}
<!-- docs-site-link:begin -->
> [!NOTE]
> You're reading this page's **source**. The rendered version — with navigation, search,
> and live type previews — is at <https://dev.hyperlink.cool/docs/gate-runtime-observer>.
<!-- docs-site-link:end -->
# Gate — runtime observer

{.draft}
**Draft** — paired with a runnable example; tip-check before treating as SSOT.

**Source:** [`examples/forms/hyperlink/gate-runtime-observer.ts`](https://github.com/nikolasstow/Hyperlink/blob/integration/examples/forms/hyperlink/gate-runtime-observer.ts)  
**Run:** `pnpm run example:gate-runtime-observer`  
**Hub:** [Examples → Hyperlink](/docs/examples#hyperlink)

## What this form shows

Live observation on `Gate.Service` via Subscribable views (`status`, `waiting`, `completed`, …). Contrast with `Gate.make` (run-only).

{.twoslash include="examples/forms/hyperlink/gate-runtime-observer.ts"}
``` ts
```
