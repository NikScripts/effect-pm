# Store migration (archived)

The old tap/bridge → Store cutover is done. See [Stores](../../guides/stores.md) for the current
model. Historical notes live under `docs/handoffs/archive/2026-07/` (store cutover reports).
