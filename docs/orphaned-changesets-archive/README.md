Orphaned changeset files moved here so they are **not** consumed by `changeset version`
(Changesets walks every `*.md` under `.changeset/`).

They described work that was already folded into prior commits or superseded by the
**0.7.0-beta.0** Process v2 release notes. Do not move them back into `.changeset/`
unless you intend to re-version the package from their contents.
