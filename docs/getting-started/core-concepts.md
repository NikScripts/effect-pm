{#core-concepts title="Core Concepts" status="draft" appliesTo=all}
# Core Concepts

{.note}
**Placeholder — outline only.** Full content to come; this page exists now so other pages can link to it.

The mental model — a resource is a **contract** plus an **implementation**; the same tag runs in-process, served over RPC, or reached as a client. Covers the core vocabulary: Tag, contract, layer, node.
