{#contracts title="Service Contracts" appliesTo=all}
# Service Contracts

{.note}
**Placeholder — outline only.** Full content to come; this page exists now so other pages can link to it.

The contract in depth — the method kinds (`effect`, `effectFn`, `ref`, `stream`), payload schemas, and how a contract validates both type and runtime.
