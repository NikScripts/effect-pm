/**
 * @packageDocumentation
 *
 * **last-ts** — Effect + React building blocks. Root barrels real modules only
 * (Effect-shaped). Product name Last.ts; a `Last` namespace lands only when it
 * has a real cross-cutting API.
 *
 * Prefer subpath imports: `last-ts/View`, `last-ts/Page`, `last-ts/AtomReact`, …
 *
 * @see docs/plans/last-ts-codesplit.md
 */
export * as Page from "./Page";
export * as AtomReact from "./AtomReact";
